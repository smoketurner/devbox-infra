# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
.terraform.lock.hcl

# Never commit variable values — they may carry secrets (e.g. oidc_client_secret)
*.tfvars
*.tfvars.json
